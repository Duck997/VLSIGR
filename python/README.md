# vlsigr (Python)

This directory builds the `vlsigr` Python extension module via **pybind11**.

## Dev install

```bash
python -m pip install -U pip
python -m pip install -e ./python
```

## Run tests

```bash
pytest -q
```


